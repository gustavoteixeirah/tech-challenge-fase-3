export enum ErrorCodeEnum {
  REQUIRED_FIELDS = 5000,
  INVALID_TRANSFER_TYPE = 5001,
  INSUFICIENT_FUNDS_TYPE = 5002,
  TRANSACTION_NOT_FOUND = 5003,
  INVALID_UPLOAD_FORMAT = 5004,
  UPLOAD_FILE_TOO_BIG = 5005
}
